import { FindRoutePipe } from './find-route.pipe';

describe('FindRoutePipe', () => {
  it('create an instance', () => {
    const pipe = new FindRoutePipe();
    expect(pipe).toBeTruthy();
  });
});
