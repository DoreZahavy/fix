import { GetSvgDirective } from './get-svg.directive';

describe('GetSvgDirective', () => {
  it('should create an instance', () => {
    const directive = new GetSvgDirective();
    expect(directive).toBeTruthy();
  });
});
